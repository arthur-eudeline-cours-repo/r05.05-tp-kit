packages:
  - "tp-kit"
  - "storybook"

# pnpm blocks postinstall scripts by default; esbuild (pulled in by
# vite/storybook) needs its native binary built to run at all.
allowBuilds:
  esbuild: true
